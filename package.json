{
  "name": "claude-board",
  "version": "1.12.0",
  "private": true,
  "description": "Visual task management board for Claude Code — manage AI-powered development tasks with a Kanban-style interface",
  "author": {
    "name": "Bahri Hirfanoglu",
    "email": "bahri@hirfanoglu.com"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "scripts": {
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "setup": "npm install && cd client && npm install",
    "tauri": "tauri",
    "tauri:dev": "npx tauri dev",
    "tauri:build": "npx tauri build",
    "lint": "cd client && npx eslint src/ --max-warnings 0",
    "lint:fix": "cd client && npx eslint src/ --fix",
    "type-check": "cd client && npx tsc --noEmit && npx tsc --noEmit -p tsconfig.node.json",
    "format": "prettier --write \"client/src/**/*.{ts,tsx}\"",
    "test": "cd client && npx vitest run",
    "format:check": "prettier --check \"client/src/**/*.{ts,tsx}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/cli": "^2.11.2",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4"
  },
  "lint-staged": {
    "client/src/**/*.{ts,tsx}": [
      "eslint --max-warnings 0"
    ]
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  }
}
