{
  "compilerOptions": {
    /* Language / emit */
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "useDefineForClassFields": true,
    "noEmit": true,

    /* Module semantics (Vite-friendly, TS7-ready) */
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": false,

    /* Path aliases (paths resolves relative to this file under moduleResolution: Bundler,
       so no deprecated baseUrl is needed) */
    "paths": { "@/*": ["./src/*"] },

    /* Type surface */
    "types": ["vite/client", "node", "vitest/globals"],
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    /* Migration complete — the tree is 100% TS, so JS interop is off. */
    "allowJs": false,

    /* Strictness */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["src"]
}
